# Local development defaults. Redis is started with `docker compose up -d redis`
# from the repository root.
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
# Redis authentication is disabled by default in the local Compose service.
REDIS_USER=default
REDIS_PASS=
